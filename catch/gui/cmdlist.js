/*
cmdlist.js written by H3L
*/

/* DEPRECATED FILE!!!
const commands = [
  { name: 'console.log(string)', description: 'Prints a message to the output.' },
  { name: 'number + number', description: 'Adds two numbers, and displays it inside the output.' },
  { name: 'alert(string)', description: 'Displays a browser alert.' }
];

document.addEventListener('DOMContentLoaded', () => {
  updateCommandList();
});

function updateCommandList() {
  const commandList = document.getElementById('commandListItems');
  commandList.innerHTML = ''; // Clear existing list
  
  commands.forEach(command => {
    const li = document.createElement('li');
    li.innerHTML = `<b>${command.name}:</b> ${command.description}`;
    commandList.appendChild(li);
  });
}
*/
